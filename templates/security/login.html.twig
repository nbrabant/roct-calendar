<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Roct Calendar</title>
    <link rel="shortcut icon" href="/favicon.ico">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center min-h-screen">
    <div class="bg-white rounded-lg shadow-md p-8 w-full max-w-sm">
        <div class="w-[30%] mx-auto">
            <img src="{{ asset('images/logo.png') }}" alt="Logo ROCT" class="max-w-full">
        </div>

        <h1 class="text-center text-2xl font-semibold text-gray-800 mt-0 mb-6">Roct Calendar</h1>

        {% for message in app.flashes('success') %}
            <div class="bg-green-100 text-green-800 border border-green-300 rounded p-3 mb-4">{{ message }}</div>
        {% endfor %}

        {% if error %}
            <div class="bg-red-100 text-red-800 border border-red-300 rounded p-3 mb-4">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        <form method="post">
            <div class="mb-4">
                <label for="input-email" class="block mb-1 font-semibold text-gray-600">Email</label>
                <input type="email" id="input-email" name="_username" value="{{ last_username }}" required autofocus
                       class="w-full px-3 py-2 border border-gray-300 rounded text-base focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500">
            </div>
            <div class="mb-4">
                <label for="input-password" class="block mb-1 font-semibold text-gray-600">Password</label>
                <input type="password" id="input-password" name="_password" required
                       class="w-full px-3 py-2 border border-gray-300 rounded text-base focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-red-500">
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <button type="submit" class="block w-full py-2.5 bg-red-600 hover:bg-red-700 text-white rounded text-base cursor-pointer mt-4">Log in</button>
        </form>

        <a href="{{ path('app_forgot_password_request') }}" class="block text-center mt-4 text-red-600 hover:underline text-sm">Mot de passe oublié ?</a>
    </div>
</body>
</html>
