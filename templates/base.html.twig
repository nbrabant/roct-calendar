<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}ROCT Calendar{% endblock %}</title>
        <link rel="shortcut icon" href="/favicon.ico">
        <script src="https://cdn.tailwindcss.com"></script>
        {% block stylesheets %}
        {% endblock %}

        {% block javascripts %}
        {% endblock %}
    </head>
    <body class="bg-gray-50 min-h-screen flex flex-col">
        <header class="bg-white shadow-sm">
            <div class="max-w-5xl mx-auto px-4 py-4 flex items-center gap-4">
                <a href="{{ path('app_home') }}">
                    <img src="{{ asset('images/logo.png') }}" alt="Logo ROCT" class="h-12 w-auto">
                </a>
                <a href="{{ path('app_home') }}" class="text-xl font-bold text-gray-800">ROCT Calendar</a>
            </div>
        </header>

        <main class="flex-1 max-w-5xl w-full mx-auto px-4 py-8">
            {% block body %}{% endblock %}
        </main>

        <footer class="bg-white border-t border-gray-200 mt-auto">
            <div class="max-w-5xl mx-auto px-4 py-4 text-center text-sm text-gray-400">
                &copy; {{ "now"|date("Y") }} ROCT
            </div>
        </footer>
    </body>
</html>
